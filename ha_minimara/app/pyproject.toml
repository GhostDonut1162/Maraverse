[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "ha-minimara"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["mcp==2.1.1"]

[project.scripts]
ha-minimara-chat-mcp = "ha_minimara.mcp_adapter:main"
